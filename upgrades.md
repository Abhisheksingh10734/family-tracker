# Later upgrades in the new version

1. add the error handling
    - if user tries to add the country which he has already added
    - if user types the wrong country name
    - if country does not exists

2. if any country name is too long and user inputs its some part then add the country in the visited list without any error.

3. (optional) add a button which shows all the countries that user have visited or added. If there is no countries then also show some message.

4. convert the user input to lower case and also country result to lowercase.

5. add multi member functionality means user can add members and for each member differnet record and also different color options.

6. It does not adds the country to other users list which any user added earlier.